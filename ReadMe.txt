usage:
install mongodb
run mongod in the terminal to startstart mongodb
open another terminal and run mongo and create a database mydb by running use db
navigate to the project folder on your terminal
run npm start to start the server
navigate to http://localhost:5000/ on your browser.
